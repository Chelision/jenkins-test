server {
    listen 80;
    server_name admin.mumup.asia;

    root /var/www/admin.mumup.asia/current;
    index index.html;

    access_log /var/log/nginx/admin.mumup.asia.access.log;
    error_log /var/log/nginx/admin.mumup.asia.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|webp|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
